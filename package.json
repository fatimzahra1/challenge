{
  "name": "backend-boilerplate",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^9.7.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "request": "^2.88.2"
  }
}
